# Naxa Mobile

> Placeholder for future React Native app (iOS + Android).

The mobile app will mirror the web editor with native touch gestures for lane
drawing, optimized for tablet use on the factory/warehouse floor.

**Planned stack:** React Native + Expo, sharing `@naxa/core` types with the web app.

This directory will be populated in a future release.
